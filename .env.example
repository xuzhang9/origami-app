# Family code for device registration
FAMILY_CODE=origami2024

# Vercel Postgres (optional - for caching and device management)
# Get this from your Vercel dashboard after creating a Postgres database
POSTGRES_URL=

# Upstash Redis (optional - for rate limiting)
# Get this from Upstash after creating a Redis database
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Note: OpenAI API key is NOT stored in environment variables
# Users provide their own API key in the Settings page
# This approach keeps costs per-user and allows public repository hosting
